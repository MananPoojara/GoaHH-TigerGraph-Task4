# Required case outputs

The implementation must generate and commit exactly one validated JSON file for each official case, `HHG-001.json` through `HHG-020.json`. Incomplete work belongs in the ignored `outputs/drafts/` directory; only files that pass the full release validator enter this directory.
