# Raw knowledge documents

Ignored source PDFs or HTML downloaded from approved regulatory URLs. Keep a checked-in manifest with URL, title, retrieval date, hash, and license/status rather than committing large source files.
