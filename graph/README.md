# TigerGraph assets

This area will contain the versioned schema, loading jobs, installed GSQL queries, algorithms, and migrations. Query contracts are defined in `docs/04-data-and-graph-model.md` before GSQL implementation begins.
