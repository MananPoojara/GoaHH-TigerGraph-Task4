# Processed load data

Deterministic, ignored files derived from the official CSVs for TigerGraph loading. Every transform must preserve source IDs and emit row counts, rejects, hashes, and the transform version.
