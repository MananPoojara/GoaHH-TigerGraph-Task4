# Raw benchmark data

Place the official `transactions.csv`, `identity.csv`, `closed_cases_history.csv`, and `case_pack.csv` files here. They are ignored by Git. Record file hashes in the run manifest; do not rename IDs or use public IEEE-CIS labels.
